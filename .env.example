# === App ===
APP_HOST=127.0.0.1
APP_PORT=8000
ENV=dev

# === Ollama (local model server) ===
OLLAMA_BASE_URL=http://127.0.0.1:11434
OLLAMA_MODEL=llama3.2:3b

# === Postgres + pgvector (optional; set if using external DB) ===
PG_HOST=127.0.0.1
PG_PORT=5432
PG_DB=rag
PG_USER=rag
PG_PASSWORD=ragpass

# === Embeddings (sentence-transformers model name) ===
EMBEDDING_MODEL=all-MiniLM-L6-v2

# === Optional judge/detector APIs (leave blank if unused) ===
OPENAI_API_KEY=
GEMINI_API_KEY=
GRANITE_API_KEY=
